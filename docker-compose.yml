version: "3.9"

services:
  backend:
    image: proffessor-backend-backend:latest
    container_name: professor-backend
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
